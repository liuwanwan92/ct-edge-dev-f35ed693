#!/usr/bin/env bash
# mock modprobe -- nestart loads the nbd kernel module; never do this for real.
_d="$(cd "$(dirname "$0")" 2>/dev/null && pwd)"; . "${_d}/mocklib.sh" 2>/dev/null || mock_log(){ :; }
mock_log "$@"
exit 0
